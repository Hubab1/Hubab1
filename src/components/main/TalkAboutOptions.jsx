import React from 'react';

export default class TalkAboutOptions extends React.Component {
    render () {
        return (
            <div>
                Let's talk
            </div>
        );
    }
}