import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

import { P } from 'assets/styles';


export default function ExistinParking({item}) {
    return <div></div>
}

ExistingParking.propTypes = { 
    item: PropTypes.object,
};
