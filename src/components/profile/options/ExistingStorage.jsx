import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

import { P } from 'assets/styles';


export default function ExistingStorage({item}) {
    return <div></div>
}

ExistingStorage.propTypes = { 
    item: PropTypes.object,
};
